export const currentNameCategory = (name)=>{
  //params.catagoryName
  if (name === 'nurseries') {
    return ['питомник', 'питомнике', 'питомник'];
  } else 
  if (name === 'animal') {
    return ['порода', 'породе', 'породе'];
  };
}

